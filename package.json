{
  "name": "hardhat-project",
  "scripts": {
    "prettier": "prettier --write 'contracts/**/*.sol'",
    "lint": "prettier --list-different 'contracts/**/*.sol'",
    "test": "npx hardhat test",
    "compile": "npmx hardhat compile",
    "deploy": "hardhat deploy --network bsc",
    "deploy-test": "hardhat deploy --network rinkeby",
    "deploy-bsc-test": "hardhat deploy --network testBSC",
    "verify": "hardhat --network rinkeby etherscan-verify --sleep",
    "verify-bsc": "hardhat --network testBSC etherscan-verify --sleep"
  },
  "devDependencies": {
    "@nomiclabs/hardhat-ethers": "^2.0.5",
    "@nomiclabs/hardhat-etherscan": "^3.0.1",
    "@nomiclabs/hardhat-waffle": "^2.0.2",
    "chai": "^4.3.6",
    "dotenv": "^16.0.0",
    "ethereum-waffle": "^3.4.0",
    "@openzeppelin/contracts": "^4.3.1",
    "ethers": "^5.5.4",
    "hardhat": "^2.8.4",
    "hardhat-deploy": "^0.10.5",
    "hardhat-deploy-ethers": "^0.3.0-beta.13",
    "hardhat-gas-reporter": "^1.0.7",
    "solidity-coverage": "^0.7.19"
  },
  "dependencies": {
    "prettier": "^2.5.1",
    "prettier-plugin-solidity": "^1.0.0-beta.19"
  }
}
